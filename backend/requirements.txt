# ============================================================
# SALESJI Backend - Minimal requirements (low-spec server safe)
# ============================================================
# Embeddings use the Gemini API (gemini-embedding-001) instead of local
# models. No ONNX, no torch, no local model downloads needed.
# Vector dimension: 384 (matching existing pgvector schema).
# ============================================================

# --- Telegram bot (job-queue extra pulls APScheduler) ---
python-telegram-bot[job-queue]==22.7

# --- Web chat HTTP service (reuses the same RAG engine) ---
fastapi==0.115.6
uvicorn==0.34.0

# --- Supabase / DB / HTTP ---
supabase==2.30.0
httpx==0.28.1

# --- Config ---
python-dotenv==1.2.2

# --- LLM + Embeddings (Gemini only — Groq removed) ---
google-genai==1.73.1

# --- Clustering for website condensation ---
numpy==2.4.4
scikit-learn==1.8.0

# --- Web scraping ---
firecrawl-py==4.22.2
beautifulsoup4==4.14.3
requests==2.33.1

# --- Document parsing (markitdown + format handlers) ---
markitdown==0.1.5
pdfminer.six==20251230
mammoth==1.11.0
python-docx==1.2.0
openpyxl==3.1.5
lxml==6.0.4
