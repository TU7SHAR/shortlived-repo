# ============================================================
# SALESJI Backend - Minimal requirements (low-spec server safe)
# ============================================================
# Heavy ML stack (torch ~1.5GB, transformers, sentence-transformers,
# faiss, langchain*, azure*, pandas, telethon, audio libs) has been
# REMOVED. Local embeddings now run on fastembed (ONNX runtime, ~150MB)
# producing the same all-MiniLM-L6-v2 384-dim vectors.
# ============================================================

# --- Telegram bot (job-queue extra pulls APScheduler) ---
python-telegram-bot[job-queue]==22.7

# --- Supabase / DB / HTTP ---
supabase==2.30.0
httpx==0.28.1

# --- Config ---
python-dotenv==1.2.2

# --- LLM providers (switchable via LLM_PROVIDER) ---
groq==0.37.1
google-genai==1.73.1

# --- Local embeddings (lightweight, ONNX) ---
# >=0.5.0 required for Python 3.13 support (older builds cap at <3.13)
fastembed>=0.5.0

# --- Clustering for website condensation ---
numpy==2.4.4
scikit-learn==1.8.0

# --- Web scraping ---
firecrawl-py==4.22.2
beautifulsoup4==4.14.3
requests==2.33.1

# --- Document parsing (markitdown + format handlers) ---
markitdown==0.1.5
pdfminer.six==20251230
mammoth==1.11.0
python-docx==1.2.0
openpyxl==3.1.5
lxml==6.0.4
